package dev.tore.schemaforge.core;

/** Container kinds known to the restock system. Used by WorldView and ContainerIndex from P4-01 on. */
public enum ContainerType {
    CHEST,
    BARREL,
    SHULKER,
    ENDER_CHEST
}
