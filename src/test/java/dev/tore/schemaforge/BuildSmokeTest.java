package dev.tore.schemaforge;

import org.junit.jupiter.api.Test;

/**
 * Empty test proving that the JUnit 5 platform is wired into {@code ./gradlew test} (P0-03).
 * Real logic tests follow with the core/ tickets (P1-03, P2-01, ...).
 */
class BuildSmokeTest {
    @Test
    void junitPlatformRuns() {
    }
}
